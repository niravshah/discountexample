apply plugin: 'java'
buildDir = 'out'

sourceSets {
  main {
    java {
      srcDir 'src'
    }
  }
  test {
    java {
      srcDir 'test'
    }
  }
}

dependencies {
  compile fileTree(dir: 'lib', include: '*.jar')
}

